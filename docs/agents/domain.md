# Domain docs

This repo uses a **single-context** domain documentation layout.

- `CONTEXT.md` at the repo root defines the ubiquitous language and canonical glossary.
- Architecture Decision Records (ADRs) live under `docs/adr/`.
